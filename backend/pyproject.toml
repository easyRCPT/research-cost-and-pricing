[project]
name = "rcpt-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dj-database-url>=3.1.2",
    "django>=6.0.7",
    "django-cors-headers>=4.9.0",
    "djangorestframework>=3.17.1",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "openpyxl>=3.1.5",
]

# RUF012 is not suitable for Django model Meta and migration declarations.
[tool.ruff.lint]
ignore = ["RUF012"]
